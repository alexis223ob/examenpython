import random;

filas = 0
for f in range(1):
    filas = int(input("Ingrese la cantidad de filas: "))
    if filas < 1:
        input("La cantidad de filas tiene que ser mayor o igual a 1, porfavor ingrese otro nro : ")

columnas = 0
for c in range(1):
    columnas = int(input("Ingrese la cantidad de columnas: "))
    if columnas < 1:
        input("La cantidad de columnas tiene que ser mayor o igual a 1, porfavor ingrese otro nro : ")